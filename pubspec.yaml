name: expense_tracker
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  shared_preferences: ^2.1.1
  http: ^1.0.0
  flutter_secure_storage: ^9.0.0
  mongo_dart: ^0.9.3
  flutter_dotenv: ^5.1.0
  percent_indicator: ^4.2.5
  flutter_credit_card: ^3.0.1    # Complete card UI
  #masked_text_formatter: ^2.0.0  # Input masking
  font_awesome_flutter: ^10.5.0  # Payment/brand icons

  # Optional but recommended (verified):
  shimmer: ^3.0.0                # Loading effects
  animations: ^2.0.7             # Pre-built animations
  liquid_progress_indicator: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add assets here if needed
  # assets:
  #   - assets/images/

  # Add custom fonts here if needed
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
